# contrib script

This folder contains script that can work with clipman. Especially `clipman_cli` interracting with clipman memory directly.


* `rofi_menu.sh` a GUI menu for mapping a keyboard shortcut with 3 actions: secure_collect, html, clear_secure
* `transform_clipboard.py` a python wrapper too modify stdin content and wrap it in a html table
